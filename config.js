/*
TradeVault config example.

Rename this file to config.js only if you decide to use a static frontend configuration.
Only put the Supabase project URL and anon/public key here.

Never put a service_role key here.
*/
window.TRADEVAULT_CONFIG = {
  supabaseUrl: "YOUR_REAL_SUPABASE_API_URL",
  supabaseAnonKey: "YOUR_REAL_PUBLISHABLE_KEY"
};
